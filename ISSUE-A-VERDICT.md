Verdict: Proposal 1 (minimal fix, reword message). `SupersetError` exists, and `client.get_dashboards()` only wraps HTTP responses via `validate_response()` (raising `SupersetError` for 4xx/5xx, including filter and auth failures), while network failures would surface as `requests` exceptions before that, so a narrow catch with keyword heuristics would change behavior and still be brittle. The codebase already has several broad `except Exception` handlers, so keeping the broad catch but making the message honest (including the underlying exception text and treating filter incompatibility as a possibility) aligns with existing conventions. This is the simplest change and least likely to introduce new failure modes while still improving the diagnostic.
